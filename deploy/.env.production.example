# ==============================================
# Bagisto Production Environment Configuration
# ==============================================
# Copy this file to .env and configure for your production environment

# Application Settings
APP_NAME="Bagisto Demo Store"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://139.180.218.27
APP_TIMEZONE=UTC
APP_LOCALE=en
LOG_CHANNEL=stack

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=bagisto
DB_USERNAME=bagisto_user
DB_PASSWORD=secure_bagisto_password_2024
DB_PREFIX=

# Cache & Session
CACHE_STORE=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Queue Configuration
QUEUE_CONNECTION=sync

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Admin Configuration
ADMIN_URL=admin
SANCTUM_STATEFUL_DOMAINS=139.180.218.27

# Product Configuration  
PRODUCT_CONFIGURATION_FIELDS=name,description,price,weight
MEDIA_DISK=public

# Security
SANCTUM_GUARD_API=sanctum

# ==============================================
# IMPORTANT SETUP STEPS:
# ==============================================
# 1. Copy this file: cp .env.production.example .env
# 2. Generate APP_KEY: docker-compose exec bagisto php artisan key:generate --force
# 3. Update APP_URL with your actual domain/IP
# 4. Change DB_PASSWORD to a secure random password (update docker-compose.yml too)
# 5. Configure mail settings for production emails
# 6. Run migrations: docker-compose exec bagisto php artisan migrate --force
# 7. Run installer: docker-compose exec bagisto php artisan bagisto:install --force
# ==============================================
